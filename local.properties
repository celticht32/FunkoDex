## FunkoDex local configuration
## This file is never committed to git — add to .gitignore

# Channel3 API key is now user-entered in Settings > Data Sources.
# No longer needed here.

# Cloudflare Worker URL for community UPC contributions (Phase F).
# Set this to your deployed Worker URL after following the setup guide
# in FunkoDex_Security_Architecture.docx Appendix A.
# Leave blank to disable community uploads (app still works without it).
# Example:
# workerUrl=https://funkodex-contrib.YOUR_ACCOUNT.workers.dev
workerUrl=
