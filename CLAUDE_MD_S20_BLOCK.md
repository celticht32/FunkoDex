**Session 20 — reports/want-list model redesign + overlay bug fix + want-list reset (code, built + installed + verified; NOT YET COMMITTED).** A reports/UX session that replaced the S19 staged report code entirely. **The four changed files are built and installed on-device and verified working, but are NOT committed/pushed — the repo still holds S19 report code. First action next session: apply/commit the four S20 files.** Core realization: the old code computed a completion RATIO against `totalInCatalog`, which is catalog COVERAGE (rows sharing a franchise/setTag string), not an authoritative set size — hence the defensive patches (`coerceAtMost(100)`, `maxOf`, an `isTracked` fork). Completion is only meaningful for a finite, closed set, so reporting was split by axis. **Set axis (`setTag`, ~13 real named sets from enrich.js SET_SUFFIXES):** finite → ratio + bar + intent pill. **Franchise axis (open-ended franchises + user-invented groupings):** count only, never a bar/%. Five distinct fixes across four files: (1) `FunkoItem.kt` — deleted `isTracked`/`displayDenominator`, added `isCompletable = level==SET && totalInCatalog>0`, dropped the `coerceAtMost` from `completionPct`. (2) `ReportsScreen.kt` — card branches on `isCompletable`; non-set groups show "N in collection" only; expander relabeled to "Show gaps". (3) `FunkoRepository.kt` — `uniqueFranchises` now counts DISTINCT OWNED franchises (2554→136); UPC fallback added to the gaps diff (`CatalogGroupingRow` gained a `upc` field; a catalog row counts owned if handle OR upc matches — recovers ~79 falsely-missing rows, since only 100/355 owned had a catalogRef but all have UPCs); `intentFor` default flipped COMPLETE→**CHERRY_PICK** (completion is opt-IN per group); `totalWanted` rewritten to **(Y−X)+Z** = Σ gaps over COMPLETE-intent groups (deduped by catalog handle, both axes) + manual wants + missing-original. (4) `DetailScreen.kt` — fixed the "Collecting" picker overlay: `SeriesIntentSection` was a Box sibling of `ViewContent` painting on top of the hero photo and stealing taps; moved into ViewContent's scroll Column, params threaded through. Group intent was already group-scoped (keyed by franchise/setTag name, not record id) so flip-one-sets-the-group already worked. On-device verified: from want=0, tap Lilo & Stitch → 42 (gaps 42), then Haunted Mansion → 60 (gaps 18, no overlap) — each opt-in adds its gap count, deduped, reconcilable. Also did a want-list RESET to zero (authoritative backup `FunkoDex_RESET_ZERO_20260706.zip`): removed 3 stored group_prefs, fixed Harry Chitwood (was wrongly `isOwned:false`), cleared the Phineas/Ezra/Gus `isMissingOriginal` flag (Chris owns the chrome 3-pack, doesn't want the standard). Locked decisions: want-list is opt-in/default-cherry-pick; bars only on the finite set axis; **invented groupings (holiday sub-lines, "*Rides") are count-only BY DESIGN forever — Chris confirmed they "grow differently" and completion tracking would be meaningless; do NOT map them to real catalog franchises.** UNRESOLVED: Star Wars fragmentation (31 owned across 12 franchise strings, incl. dupes/junk; A-collapse vs B-normalize offered, no decision). PRIMARY OPEN WORKSTREAM (funko_enrich side): the gap counts are inflated because owned figures don't UPC-match the catalog — owned Lilo & Stitch overlaps the catalog's 44 rows on 1 of 30 (not a format issue; genuinely different figures — Chris owns mainline the catalog lacks). 197 of 255 unlinked owned UPCs aren't in the catalog at all. The fix is catalog GROWTH (harvest the mainline figures Chris owns), not re-matching existing data — investigate why enrich.js's golden master omits mainline. Full detail in the S20 CLAUDE_STATE checkpoint.
